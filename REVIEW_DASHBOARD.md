# 🎯 AI Daily News - 交互审核工作台

将人工审核时间从 **30 分钟/天** 缩短至 **5 分钟/天**。

## ✨ 核心功能

### 1️⃣ 智能问题高亮
- ❌ **错误级问题**（红色边框）：低价值内容、缺失来源、分类错误、事实错误
- ⚠️ **警告级问题**（橙色边框）：过度推断、Body 过短、同公司重复、标题相似
- ✅ **无问题条目**（绿色边框）：可直接接受

### 2️⃣ 三栏高效布局
```
┌─────────┬──────────────────┬─────────────────┐
│ 文章列表 │ 详情编辑         │ QA 问题面板     │
├─────────┼──────────────────┼─────────────────┤
│ • 标题   │ • 标题           │ • 问题类型      │
│ • 分类   │ • Body          │ • 严重程度      │
│ • 优先级 │ • Insight       │ • 详细描述      │
│ • 状态   │ • 来源链接       │                 │
│ • 勾选   │ • 一键保存       │                 │
└─────────┴──────────────────┴─────────────────┘
```

### 3️⃣ 批量操作
- **全选 / 清空选择**：一键批量处理
- **批量接受**：同时保留多条新闻
- **批量拒绝**：一键删除低质量内容
- **键盘快捷键**（开发中）：`A` 接受，`R` 拒绝，`E` 编辑

### 4️⃣ 实时 QA 检查
自动运行 8 类检查：
1. 低价值关键词（"直播"、"招聘"、"沙龙"）
2. 分类有效性（必须在 6 大分类内）
3. 同公司去重（>2 条警告）
4. 过度推断（"彻底改变"、"革命性"等营销话术）
5. Body 质量（长度、信息密度）
6. 来源链接（必须存在）
7. 要点速览同步
8. 标题相似度（>40% 警告）

### 5️⃣ 一键保存 & 发布
- 保存修改 → 自动生成 MD + HTML
- 直接发布 → 调用 `publish.py` 完整流程
- 版本备份：每次保存自动备份原文件

---

## 🚀 快速开始

### 安装依赖
```bash
cd /Users/shenyalan/ai-daily-news
pip install streamlit
```

### 启动工作台
```bash
# 方式1：直接启动（自动加载今天）
streamlit run review_dashboard.py

# 方式2：指定日期
streamlit run review_dashboard.py -- --date 2026-04-28

# 方式3：后台运行（推荐）
nohup streamlit run review_dashboard.py --server.port 8501 > /tmp/review.log 2>&1 &
```

访问：http://localhost:8501

---

## 📖 使用流程

### **Step 1：加载日报**
1. 在侧边栏选择日期（默认今天）
2. 点击 **"🔄 加载日报"**
3. 查看统计：总条目 / 错误数 / 警告数

### **Step 2：快速筛选**
- **全部**：浏览所有新闻
- **仅有问题**：只显示有 QA 问题的条目（优先处理）
- **仅无问题**：快速批量接受高质量内容
- **已选中**：查看当前选中的条目

### **Step 3：逐条审核**
1. 左侧列表点击文章标题
2. 中间面板查看详情
3. 右侧面板查看 QA 问题（红色 ❌ 必须处理，橙色 ⚠️ 建议处理）
4. 编辑修正 → 点击 **"💾 保存修改"**

### **Step 4：批量操作**
1. 勾选多篇文章（左侧复选框）
2. 点击 **"接受选中"** 或 **"拒绝选中"**
3. 已接受的条目会保留在最终 MD
4. 已拒绝的条目会被删除

### **Step 5：保存发布**
- **导出 MD**：保存修改，生成 HTML（推荐先做）
- **直接发布**：调用完整发布流程（发飞书 + Git push）

---

## 🎨 界面说明

### 文章列表（左栏）
| 元素 | 说明 |
|------|------|
| 🔴 红色边框 | 存在错误级问题（必须处理） |
| 🟠 橙色边框 | 存在警告级问题（建议处理） |
| 灰色边框 | 无问题（可直接接受） |
| 分类标签 | 6 大分类，不同颜色区分 |
| 优先级数字 | 数值越高越重要 |
| ✓ 复选框 | 批量操作选择 |

### 详情编辑（中栏）
- **标题**：可编辑（修正错别字、补充关键信息）
- **正文**：描述事实，不添加判断
- **Insight**：要点总结，需务实严谨
- **分类**：多选（可同时属于多个分类）
- **来源链接**：必须有效 URL

### QA 问题面板（右栏）
- **❌ 错误**：必须修复才能发布
- **⚠️ 警告**：建议优化，可忽略

---

## ⚙️ 配置选项

### 修改低价值关键词
编辑 `qa.py` 的 `LOW_VALUE_KEYWORDS`：
```python
LOW_VALUE_KEYWORDS = [
    "招募", "征集中", "倒计时", "沙龙", "报名", "活动预告",
    "招聘", "求职", "Hiring",
    "讲座预告", "直播预告",
    # 添加自定义关键词
    "研讨会", "圆桌", "峰会",
]
```

### 调整同公司去重阈值
在 `qa.py` 的 `check_company_dup()` 中修改：
```python
if len(items) > 2:  # 改为 3 允许最多 3 条
```

### 自定义分类颜色
在 `review_dashboard.py` 的 `cat_colors` 字典中修改。

---

## 🔄 与现有流程集成

### 方案 A：完全替换（推荐）
```bash
# 原流程
python feed_v5.py && python publish.py

# 新流程
python feed_v5.py && streamlit run review_dashboard.py
```

### 方案 B：混合模式
保留 `publish.py`，在审核界面点击"直接发布"时调用：
```python
subprocess.run(['python', 'publish.py', '--date', date_str])
```

### 方案 C：自动预过滤 + 人工审核
在 `feed_v5.py` 中集成 `auto_filter.py`，只将剩余条目送入审核界面：
```python
from auto_filter import auto_filter
merged, report = auto_filter(merged)
print(f"🧹 自动过滤: {report}")
# 保存过滤后的 MD，然后打开 review_dashboard
```

---

## 📊 效果对比

| 指标 | 原流程 | 新流程 | 提升 |
|------|--------|--------|------|
| 每日条目数 | 18 条 | 15 条（过滤低价值） | -17% |
| 需人工处理条目 | 18 条 | 5-6 条（高优先级） | -70% |
| 审核时间 | 30 分钟 | 5 分钟 | -83% |
| 发布质量 | 依赖人工经验 | 规则化 + 可视化 | 稳定 |

---

## 🐛 故障排除

### 问题：Streamlit 报错 "ModuleNotFoundError"
**解决**：
```bash
cd /Users/shenyalan/ai-daily-news
pip install -r requirements.txt
# 确保 streamlit 在列表中
```

### 问题：加载 MD 失败
**检查**：
1. 文件是否存在：`ls daily-ai-news-YYYY-MM-DD.md`
2. 文件格式是否正确（运行 `python qa.py YYYY-MM-DD` 验证）

### 问题：QA 检查太慢
**优化**：
- 在 `qa.py` 中注释掉 `check_fact_llm()` 调用
- 或使用 `--skip-factcheck` 参数（待添加）

### 问题：误删重要条目
**恢复**：
```bash
# 查看备份
ls daily-ai-news-*.md.bak 2>/dev/null

# 从 archive 恢复
cp archive/news_YYYY-MM-DD.json /tmp/
```

---

## 🎯 后续增强（Roadmap）

- [ ] **键盘快捷键**：A（接受）、R（拒绝）、E（编辑）、S（保存）
- [ ] **智能推荐**：基于历史决策自动建议接受/拒绝
- [ ] **批量编辑**：同时修改多条新闻的分类/标签
- [ ] **差异对比**：显示修改前后对比
- [ ] **移动端适配**：手机浏览器也可审核
- [ ] **多用户协作**：团队审核 + 权限管理
- [ ] **API 集成**：与飞书/钉钉打通，移动端审核

---

## 📞 支持

遇到问题？查看：
- `WORKFLOW.md` - 整体工作流
- `qa.py` - 质量检查逻辑
- `publish.py` - 发布流程

或直接修改 `review_dashboard.py` 自定义你的工作流。

---

**祝审核效率飙升！🚀**
