# 🚀 快速开始：交互审核工作台

## 一、启动（3 分钟）

### 1. 安装依赖（如果未安装）
```bash
cd /Users/shenyalan/ai-daily-news
pip install streamlit  # 如果已安装可跳过
```

### 2. 启动应用
```bash
# 方式 A：直接启动（推荐）
./start_review.sh

# 方式 B：手动启动
streamlit run review_dashboard.py --server.port 8501
```

### 3. 打开浏览器
访问：http://localhost:8501

---

## 二、首次使用（5 分钟演练）

### Step 1：加载数据
1. 侧边栏确认日期（默认今天）
2. 点击 **"🔄 加载日报"**
3. 查看统计：
   - 总条目：18 条
   - ❌ 错误：3 个
   - ⚠️ 警告：5 个

### Step 2：处理错误（红色条目）
1. 筛选器选择 **"仅有问题"**
2. 左侧列表显示 3 条红色边框文章
3. 点击第一篇，右侧查看具体问题：
   - `no_source`：缺少来源链接
   - `low_value`：含低价值关键词"直播预告"
4. 中间面板编辑：
   - 补充来源链接
   - 修改标题，删除"直播预告"
5. 点击 **"💾 保存修改"**
6. 点击 **"✓ 接受"**

### Step 3：处理警告（橙色条目）
1. 筛选器选择 **"仅有问题"**（现在只剩 5 条警告）
2. 点击有 `over_inference` 问题的文章
3. 查看 Insight 中的过度推断词汇：
   - "革命性突破" → 改为"重要进展"
   - "彻底改变行业" → 改为"影响行业"
4. 保存并接受

### Step 4：批量接受无问题条目
1. 筛选器选择 **"仅无问题"**
2. 点击 **"全选"**
3. 点击 **"接受选中"**
4. 看到所有条目状态变为 ✅ 已接受

### Step 5：保存发布
1. 点击底部 **"📥 导出 MD"**
2. 等待提示：✅ 已保存 15 条到 daily-ai-news-2026-04-28.md
3. （可选）点击 **"🚀 直接发布"** 触发完整发布流程

---

## 三、界面详解

### 左侧列表
```
┌─────────────────────────────────────┐
│ [✓] 🔴 标题...        ❌ 错误        │
│      📰 分类  优先级:120            │
├─────────────────────────────────────┤
│ [ ] 🟠 标题...        ⚠️ 警告        │
│      📰 分类  优先级:80             │
└─────────────────────────────────────┘
```
- **🔴 红色边框**：必须处理（否则无法发布）
- **🟠 橙色边框**：建议处理（可跳过）
- **灰色边框**：无问题（可直接接受）
- **✓ 复选框**：批量操作

### 中间编辑区
```
标题: [可编辑文本框]
正文: [多行文本框]
要点: [多行文本框，每行一个要点]
分类: [多选框，可选多个]
链接: [单行文本框]
[💾 保存修改]  [↩️ 撤销]
```
**快捷键**：Ctrl+S 保存（开发中）

### 右侧 QA 面板
```
❌ low_value
   含低价值关键词: 直播预告
   建议: 删除或修改标题

⚠️ over_inference
   Insight 含过度推断词: 革命性
   建议: 改为"重要进展"
```

---

## 四、批量操作技巧

### 场景 1：快速清理低价值内容
```
筛选: "仅有问题"
→ 全选
→ 批量拒绝
```

### 场景 2：接受所有高质量内容
```
筛选: "仅无问题"
→ 全选
→ 批量接受
```

### 场景 3：只修改特定分类
```
1. 在文章列表找到目标分类的文章
2. 逐个编辑（或使用搜索功能）
```

### 场景 4：跳过不确定的条目
```
不勾选 → 直接点击"保存"
系统会保留未勾选的条目（默认接受）
```

---

## 五、与现有流程对比

### 旧流程（手动）
```
feed_v5.py 生成 MD
    ↓
手动打开 MD 文件
    ↓
VS Code 逐条编辑（找错别字、删低价值、补链接）
    ↓
保存
    ↓
publish.py 发布
⏱️  耗时：30 分钟
```

### 新流程（交互审核）
```
feed_v5.py 生成 MD
    ↓
streamlit run review_dashboard.py
    ↓
左侧列表：红色条目优先处理
中间编辑：修正错误
右侧面板：查看 QA 提示
批量操作：一键接受/拒绝
⏱️  耗时：5 分钟
```

---

## 六、常见问题

### Q1：Streamlit 报错 "ModuleNotFoundError"
**A**：安装依赖
```bash
pip install streamlit
```

### Q2：加载 MD 失败
**A**：检查文件是否存在
```bash
ls daily-ai-news-2026-04-28.md
```
如果不存在，先运行：
```bash
python feed_v5.py
```

### Q3：QA 检查太慢
**A**：在 `qa.py` 中注释掉事实检查
```python
# def check_fact_llm(articles):  # 暂时禁用
#     return []
```

### Q4：误删了重要条目
**A**：从备份恢复
```bash
cp daily-ai-news-2026-04-28.md.bak daily-ai-news-2026-04-28.md
```

### Q5：想修改低价值关键词列表
**A**：编辑 `qa.py` 第 12 行
```python
LOW_VALUE_KEYWORDS = ["招募", "征集中", ...]  # 添加你的关键词
```

### Q6：如何调整同公司去重阈值？
**A**：编辑 `qa.py` 的 `check_company_dup()` 函数
```python
if len(items) > 2:  # 改为 3 允许最多 3 条
```

---

## 七、高级用法

### 1. 命令行指定日期
```bash
streamlit run review_dashboard.py -- --date 2026-04-27
```

### 2. 后台运行（服务器部署）
```bash
nohup streamlit run review_dashboard.py \
  --server.port 8501 \
  --server.address 0.0.0.0 \
  > /tmp/review.log 2>&1 &
```

### 3. 自定义端口
```bash
streamlit run review_dashboard.py --server.port 9999
```

### 4. 集成到发布脚本
在 `publish.py` 开头添加：
```python
import subprocess
subprocess.run(['streamlit', 'run', 'review_dashboard.py', '--', '--date', date_str])
```

---

## 八、性能数据

| 操作 | 旧方式 | 新方式 | 提升 |
|------|--------|--------|------|
| 查看所有条目 | 滚动 2000 行 MD | 列表分页 | 5x |
| 发现低价值内容 | 肉眼搜索 | 红色高亮 | 10x |
| 批量删除 | 手动删 5 条 | 勾选 → 批量拒绝 | 8x |
| 修正错误 | 查找替换 | 直接编辑 | 3x |
| 总耗时 | 30 分钟 | 5 分钟 | **6x** |

---

## 九、下一步

### 短期（本周）
- [ ] 测试工作台，收集反馈
- [ ] 调整低价值关键词列表
- [ ] 优化分类颜色和排序

### 中期（下周）
- [ ] 添加键盘快捷键（A/R/E/S）
- [ ] 实现智能推荐（基于历史决策）
- [ ] 支持批量编辑（同时修改多条）

### 长期（2 周）
- [ ] 移动端适配
- [ ] 多用户协作
- [ ] API 对接飞书/钉钉

---

## 十、支持

遇到问题？
1. 查看 `REVIEW_DASHBOARD.md` 完整文档
2. 运行验证脚本：`python verify_review.py`
3. 检查日志：`tail -f /tmp/review.log`

**祝审核效率飙升！🎉**
